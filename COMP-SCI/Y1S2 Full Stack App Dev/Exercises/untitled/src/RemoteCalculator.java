/*
* RemoteCalculator . java
* A class to hold the main method and launch the
server and clients .
*/
// Import session
import java.lang.management.ManagementFactory;
import java.net.*;
import static java.lang.Thread.sleep;
public class RemoteCalculator {
    public static void main ( String [] args ) {
        // TO BE COMPLETED
        // Hint : Runtime.getRuntime().exec() can be used to launch a command window where the server or the client can be run.

        System.out.println(" MAIN : Finished simulation .");
        System.exit(0);
        }
}